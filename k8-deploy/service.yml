apiVersion: v1
kind: Service
metadata:
  labels:
    version: "91c3787"
  name: canary
  namespace: default
spec:
  ports:
  - name: http1
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: canary
  sessionAffinity: None
  type: LoadBalancer
